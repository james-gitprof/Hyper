<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/App_Name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HyperAppDev"
        tools:targetApi="31">
        <service
            android:name=".MapFeature.MapActivity"
            android:foregroundServiceType="location" >
        </service>


        <activity
            android:name=".UserPageFeature.Customers.Views.UserHomepage"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.HyperAppDev" />
        <activity android:name=".MapFeature.MapActivity"
            android:exported="true"
            android:label="MapActivity"
            android:theme="@style/Theme.HyperAppDev"/>
        <activity
            android:name=".AuthenticateFeature.AuthenticationViews.AuthActivities.Authentication"
            android:exported="false" />
        <activity
            android:name=".AuthenticateFeature.AuthenticationViews.AuthActivities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>